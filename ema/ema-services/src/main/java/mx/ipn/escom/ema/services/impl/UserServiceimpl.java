package mx.ipn.escom.ema.services.impl;

import mx.ipn.escom.ema.model.user.DAO.UsersDAO;
import mx.ipn.escom.ema.model.user.DAO.impl.UserDAOimpl;
import mx.ipn.escom.ema.services.UsersService;

public class UserServiceimpl implements UsersService{
	
	/*Agregar un usuario
	  public void saveUser(UserTO userTO){
	  Users user = new Users();
	  user.setUser(userTO.getUser());
	  user.setName(userTO.getName());
	  UsersDAO userDAO = new UserDAOimpl();
	  userDAO.validateUser(user);
	  }
	 */

	/*
	 * Actualizar nombre de usuario
	 * public void updateUserName(UserTO userTO, String name){
	 * Users user = new Users();
	 * user.setUser(userTO.getUser());
	 * UsersDAO userDAO = new UserDAOimpl();
	 * userDAO.updateUser(user, name);
	 * }
	 * 
	 * */

	/*
	 * Elminar usuario
	 * public void deleteUser(UserTO userTO){
	 * Users user = new Users();
	 * user.setUser(userTO.getUser());
	 * UsersDAO userDAO = new UserDAOimpl();
	 * userDAO.deleteUser(user);
	 * }
	 * */
	
	/*
	 *Buscar Usuario
	 * public UserTO findUser(UserTO userTO){
	 * Users user = new Users();
	 * UserTO userTOResult = new UserTO();
	 * user.setUser(userTO.getUser());
	 * UsersDAO userDAO = new UserDAOimpl();
	 * Users userResult = userDAO.findUser(user);
	 * userTOResult.setUserTO(userResult.getUser());
	 * return userTOResult;
	 * }
	 * */
	
}
